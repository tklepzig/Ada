---
"ada-ui": minor
---

Added the `teal` theme (`ada-ui/teal`), a palette sampled from a starship helm
console: deep console teal on near-black, with sea-green, chartreuse,
signal-gold and steel-petrol tones and a deep alert red. All button and tile
labels were verified against WCAG AA in both dark and light mode.
